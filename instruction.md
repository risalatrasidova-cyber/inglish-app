Этот документ содержит пошаговые задания и чеклисты для разработки нового веб-приложения. Используйте его как руководство, отмечая выполненные пункты.

**Как использовать:**
1. Копируйте этот чеклист для каждого нового проекта
2. Отмечайте выполненные пункты: `[x]` - выполнено, `[ ]` - не выполнено
3. Следуйте порядку выполнения (критичные задачи в начале)
4. Проверяйте каждый пункт перед переходом к следующему

---

## 🚀 Этап 0: Планирование и настройка проекта

### 0.1 Архитектура и планирование
- [ ] Определить технологический стек (Backend + Frontend)
- [ ] Создать структуру базы данных (ER-диаграмма)
- [ ] Определить API endpoints (список всех роутов)
- [ ] Спланировать модули и их зависимости
- [ ] Определить требования к безопасности
- [ ] Создать план разработки с этапами

### 0.2 Инициализация проектов
- [ ] Создать Backend проект (NestJS/Express/etc)
- [ ] Создать Frontend проект (React/Vue/etc)
- [ ] Настроить Git репозиторий
- [ ] Создать `.gitignore` файлы
- [ ] Настроить структуру папок

---

## 🔴 Этап 1: Backend - Критичные настройки (День 1-2)

### 1.1 База данных и миграции
- [ ] Установить TypeORM/Prisma/другой ORM
- [ ] **ВАЖНО:** Настроить миграции (НЕ использовать `synchronize: true`)
- [ ] Создать первую миграцию для инициализации схемы
- [ ] Настроить подключение к БД через переменные окружения
- [ ] Создать `.env.example` файл
- [ ] Протестировать подключение к БД

**Код для проверки:**
```typescript
// ✅ Правильно: synchronize: false
synchronize: false, // Всегда false!

// ✅ Правильно: использовать миграции
// package.json
"migration:generate": "typeorm migration:generate",
"migration:run": "typeorm migration:run",
"migration:revert": "typeorm migration:revert"
```

### 1.2 Безопасность - CORS
- [ ] Установить и настроить CORS
- [ ] **ВАЖНО:** Указать конкретные домены (НЕ `origin: true`)
- [ ] Настроить разные настройки для dev/prod
- [ ] Протестировать CORS с фронтенда

**Код для проверки:**
```typescript
// ✅ Правильно
app.enableCors({
  origin: process.env.FRONTEND_URL || 'http://localhost:5173',
  credentials: true,
  methods: ['GET', 'POST', 'PATCH', 'DELETE'],
});

// ❌ НЕПРАВИЛЬНО
app.enableCors({ origin: true }); // ОПАСНО!
```

### 1.3 Логирование
- [ ] Установить Winston/Pino/другой логгер
- [ ] Настроить структурированное логирование
- [ ] Создать Logger сервис/модуль
- [ ] Настроить разные уровни для dev/prod
- [ ] **ВАЖНО:** Убрать все `console.log` из кода
- [ ] Настроить логирование в файлы для production

**Код для проверки:**
```typescript
// ✅ Правильно
import { Logger } from '@nestjs/common';

export class MyService {
  private readonly logger = new Logger(MyService.name);
  
  async doSomething() {
    this.logger.log('Starting operation');
    try {
      // ...
    } catch (error) {
      this.logger.error(`Operation failed: ${error.message}`, error.stack);
    }
  }
}

// ❌ НЕПРАВИЛЬНО
console.log('Starting operation'); // Убрать!
```

### 1.4 Обработка ошибок
- [ ] Создать глобальный Exception Filter
- [ ] Создать кастомные исключения
- [ ] Настроить единообразные ответы об ошибках
- [ ] Добавить логирование ошибок
- [ ] Протестировать обработку ошибок

**Код для проверки:**
```typescript
// ✅ Глобальный Exception Filter
@Catch()
export class HttpExceptionFilter implements ExceptionFilter {
  catch(exception: unknown, host: ArgumentsHost) {
    // Обработка всех ошибок
    this.logger.error(exception);
    // Возврат единообразного ответа
  }
}
```

---

## 🟡 Этап 2: Backend - Базовые модули (День 3-5)

### 2.1 Авторизация и аутентификация
- [ ] Установить JWT/Passport
- [ ] Создать Auth модуль
- [ ] Создать User entity
- [ ] Реализовать регистрацию/логин
- [ ] Настроить хеширование паролей (bcrypt)
- [ ] Создать JWT стратегию
- [ ] Создать Guards для защиты роутов
- [ ] Протестировать авторизацию

### 2.2 Rate Limiting
- [ ] Установить `@nestjs/throttler`
- [ ] Настроить rate limiting глобально
- [ ] Настроить разные лимиты для разных эндпоинтов
- [ ] Протестировать rate limiting

**Код для проверки:**
```typescript
// ✅ Настроить rate limiting
ThrottlerModule.forRoot({
  ttl: 60,
  limit: 10,
}),
```

### 2.3 Валидация
- [ ] Установить class-validator
- [ ] Настроить глобальный ValidationPipe
- [ ] Создать DTOs для всех эндпоинтов
- [ ] Добавить валидацию в DTOs
- [ ] Протестировать валидацию

### 2.4 Базовые CRUD модули
- [ ] Создать первый модуль (например, Users)
- [ ] Создать Entity
- [ ] Создать DTOs (Create, Update, Response)
- [ ] Создать Service с бизнес-логикой
- [ ] Создать Controller с эндпоинтами
- [ ] Добавить валидацию
- [ ] Добавить обработку ошибок
- [ ] Протестировать CRUD операции

---

## 🟢 Этап 3: Backend - Дополнительные функции (День 6-8)

### 3.1 Кэширование
- [ ] Установить cache-manager или Redis
- [ ] Настроить кэширование для часто запрашиваемых данных
- [ ] Добавить инвалидацию кэша при изменениях
- [ ] Протестировать кэширование

**Код для проверки:**
```typescript
// ✅ Кэширование в сервисе
async findAll(): Promise<Entity[]> {
  const cacheKey = 'entities:all';
  const cached = await this.cacheManager.get<Entity[]>(cacheKey);
  if (cached) return cached;
  
  const entities = await this.repository.find();
  await this.cacheManager.set(cacheKey, entities, 300);
  return entities;
}
```

### 3.2 Пагинация
- [ ] Создать PaginationDto
- [ ] Реализовать пагинацию во всех списках
- [ ] Добавить метаданные (total, page, limit)
- [ ] Протестировать пагинацию

### 3.3 Поиск и фильтрация
- [ ] Реализовать поиск по текстовым полям
- [ ] Реализовать фильтрацию
- [ ] Добавить сортировку
- [ ] Протестировать поиск и фильтрацию

### 3.4 Документация API
- [ ] Установить Swagger
- [ ] Настроить Swagger модуль
- [ ] Добавить декораторы `@ApiOperation`, `@ApiResponse` ко всем эндпоинтам
- [ ] Добавить примеры запросов/ответов
- [ ] Описать все возможные ошибки
- [ ] Протестировать Swagger UI

**Код для проверки:**
```typescript
// ✅ Полная документация
@ApiOperation({ summary: 'Создание сущности' })
@ApiResponse({ status: 201, description: 'Успешно создано' })
@ApiResponse({ status: 400, description: 'Ошибка валидации' })
@Post()
async create(@Body() dto: CreateDto) {}
```

---

## 🧪 Этап 4: Backend - Тестирование (День 9-10)

### 4.1 Unit тесты
- [ ] Написать тесты для всех сервисов
- [ ] Написать тесты для критичной бизнес-логики
- [ ] Достичь минимум 70% покрытия кода
- [ ] Настроить coverage отчеты

**Код для проверки:**
```typescript
// ✅ Unit тест
describe('MyService', () => {
  let service: MyService;
  
  beforeEach(async () => {
    const module = await Test.createTestingModule({
      providers: [MyService, /* моки */],
    }).compile();
    
    service = module.get<MyService>(MyService);
  });
  
  it('should do something', async () => {
    // тест
  });
});
```

### 4.2 E2E тесты
- [ ] Написать E2E тесты для основных сценариев
- [ ] Протестировать авторизацию
- [ ] Протестировать CRUD операции
- [ ] Протестировать валидацию
- [ ] Настроить запуск тестов в CI/CD

---

## 🎨 Этап 5: Frontend - Базовая структура (День 11-13)

### 5.1 Инициализация
- [ ] Создать React/Vue проект
- [ ] Настроить TypeScript (strict mode)
- [ ] Настроить роутинг
- [ ] Настроить структуру папок
- [ ] Установить базовые зависимости

### 5.2 API интеграция
- [ ] Создать API сервис (axios/fetch)
- [ ] Настроить базовый URL из переменных окружения
- [ ] Настроить interceptors для токенов
- [ ] Настроить обработку ошибок в API сервисе
- [ ] Протестировать подключение к бэкенду

**Код для проверки:**
```typescript
// ✅ API сервис с обработкой ошибок
const api = axios.create({
  baseURL: import.meta.env.VITE_API_URL,
});

api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

api.interceptors.response.use(
  (response) => response,
  (error) => {
    // Обработка ошибок
    if (error.response?.status === 401) {
      // Перенаправление на логин
    }
    return Promise.reject(error);
  }
);
```

### 5.3 Управление состоянием
- [ ] Установить React Query/Zustand/Redux
- [ ] Настроить кэширование запросов
- [ ] Настроить инвалидацию кэша
- [ ] Протестировать работу с данными

### 5.4 Обработка ошибок
- [ ] Создать Error Boundary компонент
- [ ] Настроить обработку ошибок в запросах
- [ ] Добавить уведомления об ошибках (toast)
- [ ] Протестировать обработку ошибок

**Код для проверки:**
```typescript
// ✅ Error Boundary
class ErrorBoundary extends React.Component {
  componentDidCatch(error, errorInfo) {
    // Логирование ошибки
    console.error('Error caught:', error, errorInfo);
  }
  
  render() {
    if (this.state.hasError) {
      return <ErrorFallback />;
    }
    return this.props.children;
  }
}
```

---

## 🎨 Этап 6: Frontend - UI и компоненты (День 14-16)

### 6.1 UI библиотека
- [ ] Установить Material-UI/Tailwind/другую библиотеку
- [ ] Настроить тему
- [ ] Создать базовые компоненты (Button, Input, etc)
- [ ] Создать Layout компонент

### 6.2 Формы и валидация
- [ ] Установить react-hook-form
- [ ] Установить yup/zod для валидации
- [ ] Создать переиспользуемые компоненты форм
- [ ] Добавить валидацию во все формы
- [ ] Протестировать валидацию

**Код для проверки:**
```typescript
// ✅ Форма с валидацией
const schema = yup.object({
  name: yup.string().required('Обязательное поле'),
  email: yup.string().email('Неверный email').required(),
});

const { register, handleSubmit, formState: { errors } } = useForm({
  resolver: yupResolver(schema),
});
```

### 6.3 Компоненты
- [ ] **ВАЖНО:** Разбивать компоненты на маленькие части (макс 200-300 строк)
- [ ] Создать переиспользуемые компоненты
- [ ] Использовать композицию вместо больших компонентов
- [ ] Выносить хуки в отдельные файлы
- [ ] Протестировать компоненты

**Проверка:**
- [ ] Нет компонентов больше 300 строк
- [ ] Каждый компонент имеет одну ответственность
- [ ] Компоненты переиспользуются

---

## 🔍 Этап 7: Проверка качества кода (День 17)

### 7.1 Линтинг и форматирование
- [ ] Настроить ESLint
- [ ] Настроить Prettier
- [ ] Настроить автоматическое форматирование при сохранении
- [ ] Исправить все ошибки линтера
- [ ] Проверить отсутствие `console.log` в коде

**Проверка:**
```bash
# Запустить линтер
npm run lint

# Проверить отсутствие console.log
grep -r "console.log" src/ # Должно быть пусто (кроме тестов)
```

### 7.2 Типизация
- [ ] Включить strict mode в TypeScript
- [ ] Убрать все использования `any`
- [ ] Создать типы для всех данных
- [ ] Проверить отсутствие ошибок типизации

**Проверка:**
```bash
# Проверить типизацию
npm run type-check

# Найти все any
grep -r ": any" src/ # Должно быть минимум
```

### 7.3 Рефакторинг
- [ ] Убрать дублирование кода
- [ ] Вынести общую логику в утилиты
- [ ] Улучшить читаемость кода
- [ ] Проверить соответствие принципам SOLID

---

## 📊 Этап 8: Мониторинг и документация (День 18)

### 8.1 Мониторинг
- [ ] Настроить мониторинг ошибок (Sentry/Rollbar)
- [ ] Настроить метрики производительности
- [ ] Добавить health check эндпоинт
- [ ] Настроить алерты для критических ошибок

### 8.2 Документация
- [ ] Обновить README с инструкциями по установке
- [ ] Документировать все API эндпоинты в Swagger
- [ ] Создать документацию по архитектуре
- [ ] Добавить примеры использования
- [ ] Создать CHANGELOG

---

## 🚀 Этап 9: Деплой и CI/CD (День 19-20)

### 9.1 CI/CD Pipeline
- [ ] Настроить GitHub Actions/GitLab CI
- [ ] Добавить автоматический запуск тестов
- [ ] Добавить проверку линтера
- [ ] Настроить автоматический деплой
- [ ] Протестировать pipeline

### 9.2 Production настройки
- [ ] Настроить переменные окружения для production
- [ ] Отключить debug режимы
- [ ] Настроить логирование в файлы
- [ ] Настроить мониторинг
- [ ] Провести security audit

---

## ✅ Финальная проверка перед запуском

### Критичные проверки:
- [ ] `synchronize: false` в production
- [ ] CORS настроен правильно (не `origin: true`)
- [ ] Нет `console.log` в production коде
- [ ] Rate limiting настроен
- [ ] Обработка ошибок работает везде
- [ ] Валидация на всех уровнях
- [ ] Тесты написаны и проходят
- [ ] Документация обновлена

### Проверка безопасности:
- [ ] Пароли хешируются
- [ ] JWT токены настроены правильно
- [ ] CORS ограничен
- [ ] Rate limiting работает
- [ ] Валидация входных данных
- [ ] SQL injection защита (ORM)
- [ ] XSS защита

### Проверка производительности:
- [ ] Кэширование настроено
- [ ] Пагинация работает
- [ ] Оптимизированы запросы к БД
- [ ] Lazy loading на фронтенде
- [ ] Code splitting настроен

---

## 📝 Чеклист для каждого нового модуля

При создании нового модуля проверьте:

### Backend модуль:
- [ ] Entity создана с правильными полями
- [ ] DTOs созданы (Create, Update, Response)
- [ ] Service содержит бизнес-логику
- [ ] Controller содержит эндпоинты
- [ ] Валидация добавлена в DTOs
- [ ] Обработка ошибок реализована
- [ ] Логирование добавлено
- [ ] Swagger документация добавлена
- [ ] Unit тесты написаны
- [ ] E2E тесты написаны

### Frontend компонент:
- [ ] Компонент меньше 300 строк
- [ ] Обработка ошибок реализована
- [ ] Валидация форм добавлена
- [ ] Типизация строгая (нет `any`)
- [ ] Нет `console.log`
- [ ] Компонент переиспользуемый
- [ ] Тесты написаны

---

## 🎯 Метрики успеха

Проект готов к production, если:

- ✅ Все критичные пункты выполнены
- ✅ Покрытие тестами > 70%
- ✅ Нет ошибок линтера
- ✅ Нет `console.log` в коде
- ✅ Все компоненты < 300 строк
- ✅ Документация полная
- ✅ Security audit пройден
- ✅ Performance тесты пройдены

---

## 📚 Полезные команды для проверки

```bash
# Backend
npm run lint              # Проверка линтера
npm run test              # Запуск тестов
npm run test:cov          # Покрытие тестами
npm run build             # Сборка проекта
npm run migration:run     # Применение миграций

# Frontend
npm run lint              # Проверка линтера
npm run type-check        # Проверка типов
npm run test              # Запуск тестов
npm run build             # Сборка проекта

# Поиск проблем
grep -r "console.log" src/     # Найти все console.log
grep -r ": any" src/            # Найти все any
grep -r "synchronize.*true"     # Найти synchronize: true
```

---

## 🔄 Процесс разработки

### Ежедневные проверки:
1. Запустить линтер перед коммитом
2. Запустить тесты перед коммитом
3. Проверить отсутствие `console.log`
4. Проверить типизацию

### Еженедельные проверки:
1. Проверить покрытие тестами
2. Рефакторинг больших компонентов
3. Обновление документации
4. Code review

### Перед релизом:
1. Полный чеклист из этого документа
2. Security audit
3. Performance тесты
4. Документация обновлена

---

**Удачи в разработке! 🚀**

*Этот чеклист создан на основе анализа проекта POLIGLOT и содержит все важные пункты для создания качественного приложения.*


